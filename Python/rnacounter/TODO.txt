

For sure:

* rnaseq.py: Read annotation from fasta headers if present (regexp)
* Paired-end

Maybe:

* Sum transcript counts to get gene counts?
* "strict" to become an option? (reads at the intersection of 2 genes discarded by default now)
* Max likelihood method

